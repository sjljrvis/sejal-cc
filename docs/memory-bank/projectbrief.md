# Project Brief: [Employee Name / Use Case]

> **TEMPLATE — fill this out before writing feature code for a new use case.**
> When you fork this template for a real Employee project, replace every section below with concrete answers. The agent reads this file at session start (Rule 05) — incomplete content here means every later step works from a blurry picture of the goal.

## Business Context
- **Problem this solves:**
- **Stakeholders / end users:**
- **Business process being automated or augmented:**
- **Why now (deadline, regulatory pressure, cost driver, opportunity):**

## What We're Building
- **Dashboard (Eyes):** What insights and KPIs will be shown? Who looks at them and how often?
- **Workbench (Hands):** What records / exceptions / queues will operators handle? What does a typical operator action look like?
- **AI Engine (Brain):** What rules and policies govern AI behavior? What capabilities does the AI need (classification, extraction, summarization, recommendation, autonomous action)?

## Data Model (high-level)
- **Core entities:** (e.g., Tickets, Orders, Claims — list them)
- **Inputs the AI ingests:**
- **Outputs the AI produces:**
- **Feedback loops:** (where does human correction become future AI training signal?)

## Integrations
- **Source systems (POC and production):**
- **Authentication / identity providers:**
- **Notification channels:**
- **Other downstream systems:**

## Success Criteria
- **What does "this is working" look like?** (concrete, measurable — not vibes)
- **What's the first milestone?**
- **What does failure mode look like?** (so we can detect drift)

## Out of Scope
- (List things this Employee will NOT do, so future scope creep is easy to push back on.)
